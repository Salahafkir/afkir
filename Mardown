# Fiches Récapitulatives des Outils de Cybersécurité

## Gobuster - Outil de fuzzing pour découvrir des répertoires et fichiers cachés

### Installation
```bash
sudo apt-get install gobuster
```

### Commandes de Base
- Recherche de répertoires : `gobuster dir -u http://example.com -w wordlist.txt`
- Recherche de sous-domaines : `gobuster dns -d example.com -w wordlist.txt`

## Nikto - Scanner de serveurs web pour des failles de sécurité

### Installation
```bash
sudo apt-get install nikto
```

### Commandes de Base
- Scanner un serveur web : `nikto -h http://example.com`

## Yersinia - Outil d'attaque de protocoles réseau

### Commandes de Base
- Attaquer le protocole STP : `yersinia -G` (et suivre les instructions dans l'interface graphique)

## DirBuster - Outil de recherche de répertoires web

### Installation
```bash
sudo apt-get install dirbuster
```

### Commandes de Base
- Lancement de l'interface graphique : `dirbuster`

## Impacket - Collection de classes Python pour travailler avec des protocoles réseaux

### Installation
```bash
pip install impacket
```

### Commandes de Base
- Utilisation de `smbclient.py` : `smbclient.py username:password@example.com`

## Autres Outils Potentiellement Utiles

### Metasploit Framework
- `msfconsole` pour lancer Metasploit.
- `search exploit_name` pour chercher des exploits disponibles.

### Nmap
- `nmap -sS target_ip` pour effectuer un scan SYN.

### Wireshark
- Utiliser l'interface graphique pour analyser le trafic réseau.

### SQLmap
- `sqlmap -u "http://example.com/page?id=1"` pour tester les injections SQL.

### Aircrack-ng
- `aircrack-ng capfile.cap` pour tenter de casser une clé WEP ou WPA.

### John the Ripper
- `john --wordlist=wordlist.txt hashfile` pour casser des mots de passe.

---
